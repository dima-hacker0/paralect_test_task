import classes from "./header.module.css";

const Header = () => {
  return (
    <header className={classes.header}>
      <div className={classes.contaiene}></div>
    </header>
  );
};

export default Header;
