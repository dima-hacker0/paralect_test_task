import classes from "./header.module.css";

const Header = () => {
  return (
    <header className={classes.header}>
      <div className={classes.contaiener}></div>
    </header>
  );
};

export default Header;
